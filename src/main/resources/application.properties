# port
server.port = 9090

# database init, supports mysql
database=mysql
spring.datasource.url=jdbc:mysql://huming.gonetis.com/calendar
#spring.datasource.url=jdbc:mysql://localhost/calendar
spring.datasource.username=huming
spring.datasource.password=dkatjr1

# Uncomment these 2 lines at the first time the app runs
#spring.datasource.initialization-mode=always
#spring.datasource.schema=classpath*:${database}/schema.sql

# JPA
spring.jpa.hibernate.ddl-auto=update